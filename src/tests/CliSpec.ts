import * as chai from 'chai';
import * as sinon from 'sinon';
import {SinonStub} from 'sinon';

const assert = chai.assert;

describe(' > CliSpec', function() {
    this.timeout(2000);

    const argvPart = [
        '/usr/local/bin/node',
        '/home/remipassmoilesel/npm-root/bin/ck',
    ];

    it(' > Invalid commands should throw', () => {

    });

});

